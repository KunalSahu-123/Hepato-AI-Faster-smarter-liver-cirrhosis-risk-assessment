# LiverAI - Liver Cirrhosis Prediction System
# Python 3.11+ required (developed and tested on 3.13).
#
#   python -m venv venv
#   venv\Scripts\activate          (Windows)   |   source venv/bin/activate   (Linux/macOS)
#   pip install -r requirements.txt
#
# Versions are pinned to what this project was built and tested against.

# ---- Web framework -------------------------------------------------------
Flask==3.1.3
Werkzeug==3.1.8
Jinja2==3.1.6
itsdangerous==2.2.0            # signs the single-use password-reset tokens

# ---- Auth, forms, database ----------------------------------------------
Flask-Login==0.6.3
Flask-WTF==1.3.0               # CSRFProtect
WTForms==3.2.2
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.52
email-validator==2.3.0         # server-side email syntax + deliverability check

# ---- Configuration ------------------------------------------------------
python-dotenv==1.2.3           # loads .env in development

# ---- Machine learning ---------------------------------------------------
scikit-learn>=1.5.0,<2.0
pandas>=2.2.0,<3.1
numpy>=1.26.0,<2.5
scipy>=1.13.0,<2.0
joblib>=1.4.0                  # model / encoder serialisation

# ---- Charts and reports -------------------------------------------------
matplotlib>=3.9.0,<4.0
seaborn>=0.13.0,<1.0
reportlab>=4.1,<6.0            # PDF generation

# ---- Lab-report OCR (optional) ------------------------------------------
# Needs the Tesseract binary installed separately; the app detects its
# absence and hides the OCR upload rather than failing.
#   Windows:  https://github.com/UB-Mannheim/tesseract/wiki
#   Ubuntu:   sudo apt install tesseract-ocr poppler-utils
#   macOS:    brew install tesseract poppler
pytesseract>=0.3.10
Pillow>=10.0.0,<13.0
pdf2image>=1.16.0              # only needed to OCR multi-page PDF reports

# ---- PostgreSQL driver (production) --------------------------------------
psycopg2-binary==2.9.10

# ---- Production WSGI server ---------------------------------------------
# waitress is pure-Python and works on Windows; on Linux you may prefer
# gunicorn instead (see DEPLOYMENT.md).
waitress==3.0.2
gunicorn==23.0.0

# ---- HTTP requests (AI chatbot) -----------------------------------------
requests==2.32.3

# ---- Development / testing ----------------------------------------------
pytest==9.1.1
